{
  "mcpServers": {
    "data-platform": {
      "command": "data-platform-mcp",
      "env": {
        "AWS_PROFILE": "${AWS_PROFILE}",
        "AWS_REGION": "${AWS_REGION}",
        "DATAPLATFORM_SANDBOX_ACCOUNTS": "${DATAPLATFORM_SANDBOX_ACCOUNTS}"
      }
    }
  }
}
